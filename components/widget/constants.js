/**
 *
 * @author Karl-Johan Bailey
 * @created 20/11/2022
 * @desc Default values for widget so that all components share values
 *
 */

const defaults = {
  width: "100%",
  maxWidth: "100%",
  height: 200,
  borderRadius: 7,
  borderWidth: 0.05,
  borderColor: "background.primary",
  borderWidth: 2,
};

export default defaults;
